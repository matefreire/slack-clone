{
  "name": "slack-clone",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/slack-clone",
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "Next.js",
      "onAutoForward": "notify"
    }
  },
  "remoteUser": "node",
  "updateRemoteUserUID": false,
  "onCreateCommand": "sed 's/\\r$//' .devcontainer/fix-permissions.sh | bash",
  "postCreateCommand": "mkdir -p .convex-tmp && npm install",
  "postStartCommand": "sed 's/\\r$//' .devcontainer/fix-permissions.sh | bash",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "bradlc.vscode-tailwindcss"
      ]
    }
  }
}
